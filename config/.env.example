# Backend Environment Variables

# JWT Configuration
JWT_SECRET=your-secret-key-here
JWT_EXPIRES_IN=1h

# MySQL Database
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=your-password
MYSQL_DATABASE=mirai_db
MYSQL_PORT=3306

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend Build Serving (Production)
SERVE_FRONT=false
FRONT_DIST_PATH=../dist

# Google Gemini AI Integration
# IMPORTANTE: Gere sua chave em https://aistudio.google.com/app/apikey (NÃO use Google Cloud Console!)
# A chave deve começar com "AIzaSy" e ter ~39 caracteres
GEMINI_API_KEY=AIzaSyBxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GEMINI_MODEL=gemini-1.5-flash-latest

# Frontend WebSocket URL (optional, auto-detected in dev)
# VITE_API_WS_URL=http://localhost:5000
